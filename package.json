{
  "private": true,
  "scripts": {
    "test": "npm run lint && mocha ./node_modules/html-tests/task-stub/test && mocha --exit test/*.spec.js",
    "lint": "stylelint *.css **/*.css && htmllint *.html"
  },
  "dependencies": {
    "html-tests": "2.0.0",
    "htmllint-cli": "0.0.7",
    "mocha": "5.2.0",
    "should": "11.1.0",
    "stylelint": "7.13.0",
    "stylelint-config-hrundel": "1.0.3"
  }
}
